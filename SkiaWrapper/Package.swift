// swift-tools-version:4.2
import PackageDescription

let package = Package(
  name: "Skia",
  products: [
    .library(
      name: "Skia",
      targets: ["Skia"]),
    ],
  targets: [
    .target(
      name: "Skia",
      dependencies: [])
  ]
)
